<link rel="stylesheet" type="text/css" href="{{ 'index-service-intro.css' | asset_url }}">
<div class="index-service-intro container index-normal-component">
  <div class="intro-image-wrap">
    {% if section.settings.image != blank %}
      <img
        class="intro-image"
        src="{{ section.settings.image | image_url: width: 1600 }}"
        alt="{{ section.settings.image.alt | default: section.settings.title }}"
        loading="lazy"
        width="1600"
        height="{{ 1600 | divided_by: section.settings.image.aspect_ratio | round }}"
      >
    {% endif %}
    <div class="intro-stats">
      {% for block in section.blocks %}
        <div class="stat-item" {{ block.shopify_attributes }}>
          <div class="stat-number">{{ block.settings.stat_number }}</div>
          <div class="stat-label">{{ block.settings.stat_label }}</div>
        </div>
      {% endfor %}
    </div>
  </div>
</div>

{% schema %}
{
  "name": "BtoB Service Intro",
  "settings": [
    {
      "type": "image_picker",
      "id": "image",
      "label": "背景图片"
    }
  ],
  "blocks": [
    {
      "name": "统计项",
      "type": "stat_item",
      "settings": [
        {
          "type": "text",
          "id": "stat_number",
          "label": "数字",
          "default": "1600+"
        },
        {
          "type": "text",
          "id": "stat_label",
          "label": "描述",
          "default": "Employee"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "BtoB Service Intro",
      "blocks": [
        {
          "type": "stat_item",
          "settings": {
            "stat_number": "1600+",
            "stat_label": "Employee"
          }
        },
        {
          "type": "stat_item",
          "settings": {
            "stat_number": "20M+",
            "stat_label": "Families"
          }
        },
        {
          "type": "stat_item",
          "settings": {
            "stat_number": "70+",
            "stat_label": "Countries & Regions"
          }
        },
        {
          "type": "stat_item",
          "settings": {
            "stat_number": "330K+ m²",
            "stat_label": "Global Warehouse"
          }
        },
        {
          "type": "stat_item",
          "settings": {
            "stat_number": "1070+",
            "stat_label": "Global Patents"
          }
        }
      ]
    }
  ]
}
{% endschema %}
